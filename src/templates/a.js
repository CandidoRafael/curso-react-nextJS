  // Quando o componente atualizar
      // componentDidUpdate() { }

      // Quando o componente for desmontado
      // componentWillUnmount() { }

      // handleTimeout = () => {
      //   const { posts, counter } = this.state;
      //   posts[0].title = 'O titulo foi mudado no didMount'
      //  this.timeoutUpdate = setTimeout(() => {
      //       this.setState({ counter: counter + 1 })
      //   }, 1000);
      // }

    // handlePClick= () => {
    //   // Usado para setar outro valor ao state
    //   // Quando muda o estado a funçao é chamada dnv
    //   this.setState({ name: "Junior" })
    // }
    // // Para evitar ter que fazer essa chamada o bind(),
    // // para usar o this , se utiliza a arrow function
    // handleAClick = (e) => {
    //     e.preventDefault()  
    //   const { counter } = this.state;
         
    //     this.setState({ counter: counter + 1 })
    // }